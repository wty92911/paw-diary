# Frontend Integration Guide - Typed Activity Data

## 📋 Overview

The frontend integrates seamlessly with the typed ActivityData backend system.

**✅ No changes required to existing frontend code!**

The backend automatically handles conversion between frontend block format and typed ActivityData.

## 🔄 How It Works

Frontend sends blocks → Backend converts to typed data → Auto-updates pet profile → Returns blocks

See `docs/frontend-adapter-summary.md` for complete details.
